#include <algorithms/basic_alg.h>

namespace lod_generator{
    int lod_basic_alg(mesh& src_mesh, mesh& dst_mesh, double error){
        auto& vertexes = src_mesh.get_vertexes_link();
        auto& indices = src_mesh.get_indexes_link();

        

        return SUCCESS;
    }
    
}